package com.backend.domain;

/**
 * Unique index names we want to use in Datastore
 * <br>
 * The values must not ever be renamed, since it could
 * mean that unique values stop being unique
 */
public enum UniqueProps {
    user_email
}
